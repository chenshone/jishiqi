<import src="list-item/list-item-tpl.wxml" />
<import src="../../data/tpl/navigation.wxml" />
<template is="navigation"></template>
<view class="container">
	<view class="date-box">
		<text class="mouthDay">{{dateList[idx].date}}</text>
		<view class="year-week-box">
			<text class="year">{{dateList[idx].year}}</text>
			<text class="week">{{dateList[idx].week}}</text>
		</view>
	</view>
	<view class="line line1"></view>
	<view class="tips" >
		<text>事项时轴</text>
	</view>
	<view class="line lin2"></view>
	<view class="zhou">
		<movable-area>
			<movable-view id="block" direction="horizontal" disabled="{{disabled}}" 
			catchhtouchmove = "changeDate" catchtouchstart = "changeStart" 
			catchtouchcancel="changeCanel" catchtouchend="changeEnd" style="transform: translateX({{localEnd}}px);">
				<block wx:for="{{dateList}}">
					<view class="dataPoint"></view>
				</block>
			</movable-view>
		</movable-area>
			<image src="../../images/list/zhou.png" class="zhou-img"></image>
	</view>
	<view class="to-do">
		<view class="to-word">
			<text>今日总计</text>
		</view>
		<view class="todoNum">
			<text>: {{listNum[idx].allNum}}</text>
		</view>
	</view>
	<view class="todoDetails">
		<view class="todoDetails-word">
			<text>极其重要</text>
			<text>重要</text>
			<text>一般</text>
			<text>休闲</text>
		</view>
		<view class="todoDetails-num">
			<text>{{listNum[idx].aNum}}</text>
			<text>{{listNum[idx].bNum}}</text>
			<text>{{listNum[idx].cNum}}</text>
			<text>{{listNum[idx].dNum}}</text>
		</view>
	</view>

	<scroll-view class="content" scroll-y="true">
		<view class="middle">
			<image class="middle-img" src="../../images/list/local.png"></image>
		</view>
		<view class="content-body">
			<block wx:for="{{dateList[idx].things}}" wx:for-item="item" wx:for-index="index">
				<view class="view-left" wx:if="{{index==0}}">
					<template is="newItemTpl-2" data="{{...item}}"></template>
				</view>
				<view class="view-right pad" wx:elif="{{index==1}}">
					<template is="newItemTpl-1" data="{{...item}}"></template>
				</view>
				<view class="view-left remar" wx:elif="{{index%2==0}}">
					<template is="newItemTpl-2" data="{{...item}}"></template>
				</view>
				<view class="view-right" wx:else>
					<template is="newItemTpl-1" data="{{...item}}"></template>
				</view>
			</block>
		</view>
	</scroll-view>

	<view class="footer">
		<image class="button-new" src="../../images/list/input.png"></image>
	</view>
</view>
